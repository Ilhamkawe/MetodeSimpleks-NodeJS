<div class="container">
        <div class="row justify-content-md-center">
          
          <div class="col-md-9">
            <div class="card">
              <div class="card-body">
                <h1>Hasil Optimum</h1>      
                <table class="table">
                    <thead>
                      
                    <% for (let index = 0; index < jml.length; index++) { %>
                        <th scope="col">x<%= index+1 %></th>    
                    <% } %>
                         
                    </thead>
                    <tbody>
                      <tr>
                            <% for (let index = 0; index < jml.length; index++) { %>
                                
                                <td><%= solusi.coefficients[index]%></td>    
                           <% } %>
                             
                      </tr>
                      
                    </tbody>
                  </table>
                  <h2>Optimum : <%= solusi.optimum %> </h2>
                  <h4>Optimum(Bulat) : <%= ~~solusi.optimum %> </h2>
            </div>
          </div>
        </div>
      </div>